export type AuroraOracleContract = {
  id: number
  address: string
  chainId: string
  name: string
  rpcUrl: string
}

export type AuroraOracleToken = {
  id: number
  symbol: string
  coinGeckoAlias: string
}
